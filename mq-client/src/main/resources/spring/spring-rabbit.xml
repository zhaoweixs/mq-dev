<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:rabbit="http://www.springframework.org/schema/rabbit"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="
         http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.3.xsd
         http://www.springframework.org/schema/rabbit http://www.springframework.org/schema/rabbit/spring-rabbit.xsd
         http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd">

    <context:property-placeholder location="conf/conf.properties" file-encoding="UTF-8" />

    <rabbit:connection-factory id="connectionFactory" host="${rabbit.config.host}" username="${rabbit.config.username}" password="${rabbit.config.password}" port="${rabbit.config.port}" virtual-host="${rabbit.config.vhost}" />

    <rabbit:admin id="connectAdmin" connection-factory="connectionFactory" />

    <rabbit:queue name="my_queue" exclusive="false" auto-delete="false" durable="true" declared-by="connectAdmin" />

    <rabbit:queue name="my_queue_2" durable="true" auto-delete="false" exclusive="false" declared-by="connectAdmin" />

    <rabbit:direct-exchange name="my_exchange" durable="true" auto-delete="false" declared-by="connectAdmin">
        <rabbit:bindings>
            <rabbit:binding key="yellow" queue="my_queue"></rabbit:binding>
            <rabbit:binding key="blue" queue="my_queue_2"></rabbit:binding>
        </rabbit:bindings>
    </rabbit:direct-exchange>

    <rabbit:template id="amqpTemplate" connection-factory="connectionFactory" exchange="my_exchange" />


    <rabbit:connection-factory id="connectionFactory2" host="${rabbit.config.host}" username="${rabbit.config.username}" password="${rabbit.config.password}" port="${rabbit.config.port}" virtual-host="${rabbit.config.vhost}" />

    <rabbit:admin id="connectAdmin2" connection-factory="connectionFactory2" />

    <rabbit:queue name="my_queue_3" durable="true" auto-delete="false" exclusive="false" declared-by="connectAdmin2" />

    <rabbit:topic-exchange name="my_exchange_2" durable="true" auto-delete="false" declared-by="connectAdmin2">
        <rabbit:bindings>
            <rabbit:binding pattern="color.*" queue="my_queue_3"></rabbit:binding>
        </rabbit:bindings>
    </rabbit:topic-exchange>

    <rabbit:template id="amqpTemplate2" connection-factory="connectionFactory2" exchange="my_exchange_2" />
</beans>